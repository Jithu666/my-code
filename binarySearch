class Solution {
    public int search(int[] nums, int target) {
        boolean flag = false;
        int i = 0;
        for(i = 0; i < nums.length; i++){
            if(nums[i] == target)
                flag = true;
                if(flag == true)
                    return i;
        }
            return -1;
    }
}
